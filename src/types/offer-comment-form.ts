export type TOfferFromProps = {
    offerId: string;
  };

export type TFormData = {
    rating: number ;
    review: string;
  };

export type TRatingOption = {
    value: string;
    title: string;
  };
